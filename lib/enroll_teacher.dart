import 'package:flutter/material.dart';

class EnrollTeacher extends StatelessWidget {
  const EnrollTeacher({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Enroll Teacher'),
      ),
    );
  }
}
